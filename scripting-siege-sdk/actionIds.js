ids = {

    attack: Buffer.from('Attack').toString('base64'),
    backward: Buffer.from('Backward').toString('base64'),
    forward: Buffer.from('Forward').toString('base64'),
    left: Buffer.from('Left').toString('base64'),
    right: Buffer.from('Right').toString('base64'),
    wait: Buffer.from('Wait').toString('base64')

};

module.exports = {

    actionIds: ids

};
